# Build output
Build/
*.3gx
*.elf
*.o
*.d

# The shiny jingle (assets/shiny_jingle.bcwav) IS committed -- it is generated
# deterministically by assets/make_jingle.py and shipped to users in the CI
# artifact, so it is a tracked deliverable, not build junk.
*.wav

# macOS
.DS_Store
